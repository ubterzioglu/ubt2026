# Plan: UI/UX Refactoring for Personal Website

## Overview
Refactor and improve the personal website with modern, responsive, clean design following mobile-first principles.

---

## 1. Header (Navigation)

**File:** `components/site-header.tsx`

### Changes:
- Change all nav link text color from `text-ink/70` to `text-ink` (black)
- Mobile responsive state (md:hidden):
  - Move menu trigger button to **top-right** using `ml-auto` or flexbox ordering
  - Replace full navigation with minimal label: **"UBT"** only (remove "/ Menu")
- Ensure clean alignment with consistent spacing

---

## 2. Hero Section

**File:** `components/hero-section.tsx`

### Changes:
- **Remove elements:**
  - "Personal Information Architecture" eyebrow text
  - "Explore Tools" button (keep only "View My CV" and "Contact Me")
- **Fix responsive behavior:**
  - Prevent text/buttons from overlapping profile image on smaller screens
  - Adjust flex/grid layout to ensure proper spacing on mobile
  - Add responsive padding/margins to push text away from image area
- Keep only: name, role, intro, "View My CV" button, "Contact Me" button

---

## 3. Section Order

**File:** `app/page.tsx`

### Changes:
- Reorder sections so **About Me** is second (right after Hero)
- New order: Hero → About Me → CV → Key Achievements → Tools → Tech Stack → Experience → Corporate Projects → Private Projects → Articles → Bookmarks → Useful Apps → Contact

---

## 4. About Me Section

**File:** `app/page.tsx` - about-me section

### Changes:
- Add green horizontal separator under title (same as Private Projects):
  ```jsx
  <div className="mt-6 h-px w-full bg-gradient-to-r from-accent/60 via-accent/30 to-transparent" />
  ```
- **Remove:**
  - "Positioning" eyebrow text
  - "The core positioning..." description paragraph
  - UBT 15+ years box (the dark box with "UBT", "15+", "years of quality work")
  - Description text about profile pages
- **Keep:** Only the `aboutParagraphs` content

---

## 5. CV Section (id="my-cv")

**File:** `app/page.tsx` - my-cv section

### Changes:
- Add green horizontal separator under title (same style as Private Projects)
- Keep only the two PDF buttons (English CV, German CV)
- **Remove:**
  - "Recruiter-ready" eyebrow text
  - "This section keeps..." description text
  - "View or download" button inside each card
- Simplify to minimal two-button layout

---

## 6. General UI/UX Improvements

### Across all sections:
- Ensure consistent spacing, padding, alignment
- Mobile-first responsive design using Tailwind breakpoints (`sm`, `md`, `lg`)
- Use flexbox/grid for layout stability
- Prevent text/image overlaps (CLS issues)
- Minimal, clean, startup-style landing page

### Separator consistency:
All sections with titles should have the green gradient separator style:
```jsx
<div className="mt-6 h-px w-full bg-gradient-to-r from-accent/60 via-accent/30 to-transparent" />
```

### Sections needing separators added:
- About Me (after title)
- My CV (after title)
- Key Achievements
- Tech Stack
- Experience
- Corporate Projects
- Tools Developed
- Articles
- My Bookmarks
- Useful Apps

---

## Implementation Order

1. **SiteHeader** - Fix black text color, move mobile button to right, change label to "UBT" only
2. **HeroSection** - Remove eyebrow, remove Explore Tools button, fix responsive image overlap
3. **page.tsx** - Reorder sections (About Me to position 2)
4. **page.tsx** - Simplify About Me: remove eyebrow, remove UBT box, keep paragraphs only, add separator
5. **page.tsx** - Simplify CV: add separator, keep only two buttons, remove description/eyebrow
6. **page.tsx** - Add green separators to all sections for consistency

---

## Notes
- No changes to data layer or Supabase logic
- No new components needed (can reuse existing patterns)
- Keep fallback content data in `content/featured.ts` and `content/profile.ts`
- Run `npm run lint` and `npm run typecheck` after changes